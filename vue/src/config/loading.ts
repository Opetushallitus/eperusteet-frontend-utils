/** Uses --p-primary-color set in app.scss and updated per toteutus in router.afterEach. */
export const loadingOptions = {
  isFullPage: true,
  color: 'var(--p-primary-color)',
  loader: 'dots',
};
